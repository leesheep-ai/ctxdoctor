name: ctxdoctor
description: Audit coding agent instructions for drift and broken references
runs:
  using: composite
  steps:
    - shell: bash
      run: python -m pip install .
    - shell: bash
      run: ctxdoctor .
branding:
  icon: check-circle
  color: blue
